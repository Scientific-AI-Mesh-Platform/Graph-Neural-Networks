"""
mesh_core package.
"""

import logging
from typing import Any, List, Dict

# Configure a default null handler to prevent 'No handler found' warnings
logging.getLogger(__name__).addHandler(logging.NullHandler())
